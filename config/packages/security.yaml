security:
    encoders:
        App\Entity\User:
            algorithm: auto

    # https://symfony.com/doc/current/security.html#where-do-users-come-from-user-providers
    providers:
        # used to reload user from session & other features (e.g. switch_user)
        app_user_provider:
            entity:
                class: App\Entity\User
                property: email
    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false
        main:
            anonymous: true
            lazy: true
            provider: app_user_provider
            guard:
                authenticators:
                    - App\Security\LoginFormAuthenticator
            logout:
                path: app_logout
                # where to redirect after logout
                target: app_login

            # activate different ways to authenticate
            # https://symfony.com/doc/current/security.html#firewalls-authentication

            # https://symfony.com/doc/current/security/impersonating_user.html
            # switch_user: true

    # Easy way to control access for large sections of your site
    # Note: Only the *first* access control that matches will be used
    access_control:
        
          - { path: ^/login, roles: IS_AUTHENTICATED_ANONYMOUSLY }
          - { path: ^/dashboard, roles: [ROLE_USER, ROLE_SUPER, ROLE_ADMIN] }
          - { path: ^/envios/index, roles: [ROLE_ADMIN] }
          - { path: ^/envios/edit, roles: [ROLE_ADMIN, ROLE_USER] }
          - { path: ^/envios/view, roles: [ROLE_ADMIN, ROLE_USER, ROLE_SUPER] }
          - { path: ^/envios/add, roles: [ROLE_USER] }
          - { path: ^/consulta_fnacional/user, roles: [ROLE_USER] }
          - { path: ^/consulta_fnacional/admin, roles: [ROLE_ADMIN] }


        # - { path: ^/admin, roles: ROLE_ADMIN }
        # - { path: ^/profile, roles: ROLE_USER }
