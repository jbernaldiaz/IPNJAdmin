{% extends 'base.html.twig' %}

{% block body %}
	{{ parent() }}
		<div class="container">
			<div class="row well">
				<div class="page-header margin-none" >	
					<h2 class="padding-none">Listado de Iglesias por Zonas</h2>
				</div>
				<div class="col-md-4">
					<div class="table-responsive">
						<table class="table table-striped table-bordered">
							<thead>
								<tr>
									<th>Iglesias Zona Norte</th>
									<th>Información</th>
								</tr>
							</thead>
							<tbody>
								{% for norte in norte %}
									<tr>			
										<td>{{ norte.iglesia }}</td>
										<td class="action">
									<a href="{{ path('viewUser', { id: norte.id }) }}" class="btn btn-sm btn-info">Ver</a>
									{% if is_granted('ROLE_ADMIN') %}
									<a href="{{ path('editUser', { id: norte.id }) }}" class="btn btn-sm btn-danger">Editar</a>
								{% endif %}
								</td>
									</tr>
								{% endfor %}
							</tbody>
						</table>
					</div>
				</div>
				<div class="col-md-4">
					<div class="table-responsive">
						<table class="table table-striped table-bordered">
							<thead>
								<tr>
									<th>Iglesias Zona Centro</th>
									<th>Información</th>
								</tr>
							</thead>
							<tbody>
								{% for centro in centro %}
									<tr>								
										<td>{{ centro.iglesia }}</td>
										<td class="action">
									<a href="{{ path('viewUser', { id: centro.id }) }}" class="btn btn-sm btn-info">Ver</a>
									{% if is_granted('ROLE_ADMIN') %}
									<a href="{{ path('editUser', { id: centro.id }) }}" class="btn btn-sm btn-danger">Editar</a>
								{% endif %}
								</td>
									</tr>
								{% endfor %}
							</tbody>
						</table>
					</div>
				</div>

				<div class="col-md-4">
					<div class="table-responsive">
						<table class="table table-striped table-bordered">
							<thead>
								<tr>
									<th>Iglesia Zona Sur</th>
									<th>Información</th>
								</tr>
							</thead>
							<tbody>
								{% for sur in sur %}
									<tr>							
										<td>{{ sur.iglesia }}</td>
										<td class="action">
									<a href="{{ path('viewUser', { id: sur.id }) }}" class="btn btn-sm btn-info">Ver</a>
									{% if is_granted('ROLE_ADMIN') %}
									<a href="{{ path('editUser', { id: sur.id }) }}" class="btn btn-sm btn-danger">Editar</a>
								{% endif %}
								</td>
									</tr>
								{% endfor %}
							</tbody>
						</table>
					</div>
				</div>{#
{% if is_granted('ROLE_ADMIN') %}
				<div class="col-md-4">
					<div class="table-responsive">
						<table class="table table-striped table-bordered">
							<thead>
								<tr>
									<th>Usuario</th>
									<th>Rol</th>
									<th>Información</th>
								</tr>
							</thead>
							<tbody>
								{% for role in role %}
									<tr>							
										<td>{{ role.iglesia }}</td>
										<td>{% if role.role == 'ROLE_ADMIN' %}
                                        <strong>Administrador</strong>
                                    {% elseif role.role == 'ROLE_USER' %}
                                        <strong>Usuario</strong>
                                    {% elseif role.role == 'ROLE_SUPER' %}
                                        <strong>Supervisor</strong>
                                    {% endif %}</td>
										<td class="action">
									<a href="{{ path('ipnj_iglesias_view', { id: role.id }) }}" class="btn btn-sm btn-info">Ver</a>
									{% if is_granted('ROLE_ADMIN') %}
									<a href="{{ path('ipnj_iglesias_edit', { id: role.id }) }}" class="btn btn-sm btn-danger">Editar</a>
								{% endif %}
								</td>
									</tr>
								{% endfor %}
							</tbody>
						</table>
					</div>
				</div>
			{% endif %}


{# % if is_granted('ROLE_ADMIN') % 
				<div class="col-md-4">
					<div class="table-responsive">
						<table class="table table-striped table-bordered">
							<thead>
								<tr>
									<th>Usuario</th>
									<th>Rol</th>
									<th>Información</th>
								</tr>
							</thead>
							<tbody>
								{% for roles in roles %}
									<tr>							
										<td>{{ roles.iglesia }}</td>
										<td>{% if roles.roles == 'ROLE_ADMIN' %}
                                        <strong>Administrador</strong>
                                    {% elseif roles.roles == 'ROLE_USER' %}
                                        <strong>Usuario</strong>
                                    {% elseif roles.roles == 'ROLE_SUPER' %}
                                        <strong>Supervisor</strong>
                                    {% endif %}</td>
										<td class="action">
									<a href="{{ path('viewUser', { id: roles.id }) }}" class="btn btn-sm btn-info">Ver</a>
									{% if is_granted('ROLE_ADMIN') %}
									<a href="{{ path('editUser', { id: roles.id }) }}" class="btn btn-sm btn-danger">Editar</a>
								{% endif %}
								</td>
									</tr>
								{% endfor %}
							</tbody>
						</table>
					</div>
				</div>
			% endif %#}

<div class="col-md-4 col-md-offset-3">
{% if is_granted('ROLE_ADMIN') %}
		<center>
				<a href="{{ path('addUser') }}" class="btn btn-primary">
					<span class="glyphicon glyphicon-edit"></span>
		        	{{ 'Agregar Usuario' }}
				</a> 
				<a href="{{ path('addZona') }}" class="btn btn-primary btn-info">
					<span class="glyphicon glyphicon-edit"></span>
		        	{{ 'Agregar Zona' }}
		        </a>
		    </center>
		{% endif %}
		    </div>
		   	</div>

		</div>
			
{% endblock %}
