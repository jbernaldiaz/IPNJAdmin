{% extends "base.html.twig" %}

{% block body %}
	{{ parent() }}

<div class="container">
				<div class="row well">

		<div class="col-md-12">
				<h3 class="padding-none"> Detalles de los Envios A la Tesoreria Nacional</h3>

			<div class="table-responsive">
				<p>Total records: 	</p>
				<table class="table table-striped table-hover">
					<thead>
				<tr>
					<th>id</th>
					<th>Iglesia</th>
					<th>Fecha</th>
					<th>mes</th>
                    <th>Año</th>
                    <th>operacion</th>
                    <th>Cajero</th>
                    <th>Total</th>
                    <th>Detalle Enviado</th>
                    <th>Acciones</th>
						</tr>
			</thead>
			<tbody>
				{% for envio in envio %}

                <tr>
                    <td>{{ envio.id }}</td>
                    <td>{{ envio.user }}</td>
                    <td>{{ envio.fecha|date('d-m-Y') }}</td>
                    <td>{{ envio.mes }}</td>
                    <td>{{ envio.anio|date('Y') }}</td>
                    <td>{{ envio.operacion }}</td>
                    <td>{{ envio.cajero }}</td>
                    <td>{{ envio.total }}</td>
                    <td>{{ envio.createAt|date('d-m-Y  -  H:i') }}</td>
                     <td class="action">
					<a href="{{ path('viewEnvio', { id: envio.id }) }}" >Ver</a>
                            <a href="{{ path('editEnvio', { id: envio.id }) }}">Editar</a>
                        <a>Excel</a>
                        
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
		</table>
	</div>
    </div>
    </div>

{% endblock %}
